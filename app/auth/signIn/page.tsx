"use client"
import React, { useRef } from "react";

const Login = () => {
    const userName = useRef("");
    const password = useRef("");
    
    return (
        <div>Login</div>
    )
};

export default Login;